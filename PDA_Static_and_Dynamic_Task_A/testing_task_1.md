### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame ### add an end for the class ###


  def checkforAce(card) ### change name to check_for_ace ###
    if card.value = 1 ### add = ###
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2) ### change dif to def, add a comma inbetween card1 and card2 ###
  if card1.value > card2.value
    return card.name ### add 1 and take away name ###
  else
    card2
  end
end
end ### extra end ###

def self.cards_total(cards)
  total ### add = 0 ###
  for card in cards
    total += card.value
    return "You have a total of" + total
  end
end


```
